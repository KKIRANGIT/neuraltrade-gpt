// Trend Indicators:
// EMA(9), EMA(20), EMA(50), EMA(200) — Wilder exponential smoothing
// SMA(20), SMA(50) — simple moving average
// Supertrend(10,3) — ATR-based trend line with direction flag
// ADX(14), +DI, -DI — Wilder directional movement index
// EMA Stack Score (0-4) — count of EMAs close is above
// HHHL detector — higher high higher low structure tracker
// ema_200_ready flag — true when 250+ candles available
