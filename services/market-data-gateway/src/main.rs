// NEURALTRADE — Market Data Gateway
// Entry point. Initialize config, connect to broker WebSocket,
// start tick receiver, publish ticks to SPSC ring buffer.
// Then notify candle-engine via Kafka topic: raw.ticks
fn main() {}
